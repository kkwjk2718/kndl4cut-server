<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>큰들네컷</title>
    <style>
        /* 전체 화면 스타일 */
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f3f3f3; /* 배경 회색 */
            color: #333;
            text-align: center;
        }

        /* 상단 헤더 컨테이너 */
        .header-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 20px;
        }

        /* 학교 로고 */
        .logo {
            width: 80px; /* 로고 크기 */
            height: 80px;
            margin-right: 15px;
        }

        /* 사이트 타이틀 */
        .site-title {
            font-size: 2.8rem;
            font-weight: bold;
            color: #000;
            letter-spacing: 2px;
        }

        /* 사진 컨테이너 */
        .photo-container {
            position: relative;
            width: 90%;
            max-width: 400px;
            margin: 10px 0;
            border: 2px solid #000;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            background-color: #fff;
        }

        .photo-container img {
            width: 100%;
            height: auto;
            display: block;
        }

        /* 타임스탬프 */
        .timestamp {
            font-size: 1rem;
            color: #555;
            margin-top: 15px;
        }

        /* 제작자 정보 */
        .made-by {
            font-size: 1rem; /* 조금 키움 */
            color: #000;
            font-weight: bold;
            margin-top: 10px;
        }

        /* 다운로드 버튼 */
        .button {
            display: inline-block;
            margin-top: 15px;
            padding: 12px 20px;
            font-size: 1rem;
            font-weight: bold;
            background-color: #000;
            color: #fff;
            text-decoration: none;
            border-radius: 10px; /* 버튼 둥글게 */
            transition: background-color 0.3s;
        }

        .button:hover {
            background-color: #444; /* 어두운 회색 */
        }
    </style>
</head>
<body>
    <!-- 상단: 학교 로고 + 사이트명 -->
    <div class="header-container">
        <img src="/images/gshslogo.png" alt="학교 로고" class="logo">
        <div class="site-title">큰들네컷</div>
    </div>

    <!-- 사진 컨테이너 -->
    <div class="photo-container">
        <img src="<%= photoUrl %>" alt="Photo">
    </div>

    <!-- 제작자 정보 -->
    <div class="made-by">Made by 김건우 전지윤</div>

    <!-- 다운로드 버튼 -->
    <a href="<%= photoUrl %>" download class="button">사진 다운로드</a>

    <!-- 타임스탬프 -->
    <p class="timestamp"><%= timestamp %></p>

    
</body>
</html>
